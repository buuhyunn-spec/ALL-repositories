# Dashboard environment — copy to .env.local
# API server URL (default: localhost:3000)
API_BASE_URL=http://localhost:3000/api

# Must match API_SECRET in the API server's .env
API_SECRET=your-internal-api-secret
